import { boot } from 'quasar/wrappers'

export default boot( async ({ app, ssrContext, router, store }) => {
  // app.component("SkeletonTwitch", () => import('./skeleton-twitch.vue'))
  // app.component("SkeletonTwitter", () => import('./skeleton-twitter.vue'))
})
